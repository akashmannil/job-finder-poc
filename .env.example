# JobMatch runs fully offline on predefined, deterministic engines — no API keys
# or environment variables are required. This file is kept as a placeholder.
#
# If you later add an AI-backed engine (matching / assessment / decisions), this is
# where its key would live, e.g.:
# ANTHROPIC_API_KEY=
